import { PlaceholderPage } from "@/components/portal/PlaceholderPage";

export default function CustomerSettingsPage() {
  return <PlaceholderPage title="Account settings" description="Profile, preferred language, password reset, marketing consent, data deletion request, and account deletion request will live here." actions={["Update profile", "Request data deletion"]} />;
}
