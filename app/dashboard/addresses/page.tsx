import { PlaceholderPage } from "@/components/portal/PlaceholderPage";

export default function CustomerAddressesPage() {
  return <PlaceholderPage title="Addresses" description="Saved addresses will include garden size, commune, access notes, water access, stairs/narrow access, parking notes, and default address controls." actions={["Add address"]} />;
}
