import { PlaceholderPage } from "@/components/portal/PlaceholderPage";

export default function CustomerBookPage() {
  return <PlaceholderPage title="Book new service" description="Phase 1 placeholder for choosing a service, selecting a saved address, using the pricing calculator, picking a preferred date, and submitting a requested booking for admin confirmation." actions={["Start booking request", "Add address", "Open pricing calculator"]} />;
}
