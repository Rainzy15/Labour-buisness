import { PlaceholderPage } from "@/components/portal/PlaceholderPage";

export default function CustomerSupportPage() {
  return <PlaceholderPage title="Support" description="Support will allow booking-specific messages, general support threads, response status, and notification placeholders." actions={["New support message"]} />;
}
