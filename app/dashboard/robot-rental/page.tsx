import { PlaceholderPage } from "@/components/portal/PlaceholderPage";

export default function CustomerRobotRentalPage() {
  return <PlaceholderPage title="Robot mower rental" description="Customers will see active rental period, monthly price, setup option, maintenance plan, deposit status, extension request, maintenance request, and pickup/return request." actions={["Start rental request", "Request maintenance", "Extend rental"]} />;
}
