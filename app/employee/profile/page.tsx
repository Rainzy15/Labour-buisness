import { PlaceholderPage } from "@/components/portal/PlaceholderPage";

export default function EmployeeProfilePage() {
  return <PlaceholderPage title="Employee profile" description="Profile placeholder with phone, email, skills, assigned schedule, completed jobs, and basic account settings." actions={["Update profile"]} />;
}
