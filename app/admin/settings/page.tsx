import { PlaceholderPage } from "@/components/portal/PlaceholderPage";

export default function AdminSettingsPage() {
  return <PlaceholderPage title="Business settings" description="Core settings placeholder for pricing defaults, roles, cancellation policy, privacy policy, notification templates, and future payment integration." actions={["Edit settings"]} />;
}
