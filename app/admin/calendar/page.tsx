import { PlaceholderPage } from "@/components/portal/PlaceholderPage";

export default function AdminCalendarPage() {
  return <PlaceholderPage title="Operations calendar" description="Day, week, and month calendar placeholder with color-coded bookings, employee assignments, status badges, and click-to-open booking detail." actions={["Create booking", "Assign employee"]} />;
}
